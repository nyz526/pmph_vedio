<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bc.pmph_vedio.dao.PmphUserDao">

	<!-- 根据用户id获取用户 -->
	<select id="getPmphUserById" resultType="PmphUser">
		select * from pmph_user where id = #{id}
	</select>

</mapper>
